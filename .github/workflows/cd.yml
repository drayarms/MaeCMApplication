#Works
#name: CD-Deploy

#on:
  #workflow_run:
    #workflows: ["CI-Build"]
    #types: [completed]

#permissions:
  #contents: read
  #actions: read  # Needed to download workflow metadata if necessary

#jobs:
  #deploy:
    ## Run only if the triggering CI workflow succeeded
    #if: github.event.workflow_run.conclusion == 'success'
    #runs-on: ubuntu-latest

    #steps:
      ## 1️⃣ Checkout repo (needed for .env and docker-compose.yml)
      #- name: Checkout
        #uses: actions/checkout@v4

      ## 2️⃣ Load environment variables
      #- name: Load .env
        #run: |
          #set -a
          #source .env
          #set +a
          #echo "ECR_REGISTRY=$ECR_REGISTRY" >> $GITHUB_ENV
          #echo "ECR_REPO=$ECR_REPO" >> $GITHUB_ENV
          #echo "AWS_REGION=$AWS_REGION" >> $GITHUB_ENV

      ## 3️⃣ Recompute deterministic image tags based on commit SHA
      #- name: Compute image tags
        #run: |
          #FRONTEND_TAG=$(git rev-parse --short $GITHUB_SHA)-frontend
          #BACKEND_TAG=$(git rev-parse --short $GITHUB_SHA)-backend
          #echo "FRONTEND_TAG=$FRONTEND_TAG" >> $GITHUB_ENV
          #echo "BACKEND_TAG=$BACKEND_TAG" >> $GITHUB_ENV

      ## 4️⃣ Upload files to EC2
      #- name: Upload files to EC2
        #uses: appleboy/scp-action@v0.1.7
        #with:
          #host: ${{ secrets.EC2_HOST }}
          #username: ${{ secrets.EC2_USER }}
          #key: ${{ secrets.EC2_SSH_KEY }}
          #source: "docker-compose.yml,.env"
          #target: "/home/${{ secrets.EC2_USER }}/app"

      ## 5️⃣ SSH into EC2 and deploy
      #- name: Deploy on EC2
        #uses: appleboy/ssh-action@v0.1.10
        #with:
          #host: ${{ secrets.EC2_HOST }}
          #username: ${{ secrets.EC2_USER }}
          #key: ${{ secrets.EC2_SSH_KEY }}
          #script: |
            #APP_DIR="/home/${{ secrets.EC2_USER }}/app"
            #cd $APP_DIR

            ## Load env variables
            #set -a
            #[ -f .env ] && source .env
            #set +a

            ## Use deterministic image tags
            #FRONTEND_TAG=$(git rev-parse --short $GITHUB_SHA)-frontend
            #BACKEND_TAG=$(git rev-parse --short $GITHUB_SHA)-backend

            ## Authenticate Docker to ECR
            #aws ecr get-login-password --region $AWS_REGION \
              #| docker login --username AWS --password-stdin $ECR_REGISTRY

            ## Pull images from ECR
            #docker compose --env-file .env pull

            ## Deploy containers
            #docker compose --env-file .env up -d --remove-orphans

            ## Optional cleanup
            #docker image prune -f




name: CD-Deploy

on:
  workflow_run:
    workflows: ["CI-Build"]
    types: [completed]

permissions:
  contents: read
  actions: read

jobs:
  deploy:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo (needed for .env and docker-compose.yml)
      - name: Checkout
        uses: actions/checkout@v4

      # 2️⃣ Load environment variables from .env
      - name: Load .env
        run: |
          set -a
          [ -f .env ] && source .env
          set +a
          echo "ECR_REGISTRY=$ECR_REGISTRY" >> $GITHUB_ENV
          echo "ECR_REPO=$ECR_REPO" >> $GITHUB_ENV
          echo "AWS_REGION=$AWS_REGION" >> $GITHUB_ENV

      # 3️⃣ Compute deterministic image tags in GitHub Actions
      - name: Compute image tags
        run: |
          FRONTEND_TAG=$(git rev-parse --short $GITHUB_SHA)-frontend
          BACKEND_TAG=$(git rev-parse --short $GITHUB_SHA)-backend
          echo "FRONTEND_TAG=$FRONTEND_TAG" >> $GITHUB_ENV
          echo "BACKEND_TAG=$BACKEND_TAG" >> $GITHUB_ENV

      # 4️⃣ Upload docker-compose.yml and .env to EC2
      - name: Upload files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "docker-compose.yml,.env"
          target: "/home/${{ secrets.EC2_USER }}/app"

      # 5️⃣ SSH into EC2 and deploy Docker images
      - name: Deploy on EC2
        uses: appleboy/ssh-action@v0.1.10
        env:
          ECR_REGISTRY: ${{ env.ECR_REGISTRY }}
          ECR_REPO: ${{ env.ECR_REPO }}
          AWS_REGION: ${{ env.AWS_REGION }}
          FRONTEND_TAG: ${{ env.FRONTEND_TAG }}
          BACKEND_TAG: ${{ env.BACKEND_TAG }}
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            APP_DIR="/home/${{ secrets.EC2_USER }}/app"
            cd $APP_DIR

            echo "===== Environment Variables ====="
            echo "ECR_REGISTRY=$ECR_REGISTRY"
            echo "ECR_REPO=$ECR_REPO"
            echo "AWS_REGION=$AWS_REGION"
            echo "FRONTEND_TAG=$FRONTEND_TAG"
            echo "BACKEND_TAG=$BACKEND_TAG"

            echo "===== Docker Login to ECR ====="
            aws ecr get-login-password --region $AWS_REGION \
              | docker login --username AWS --password-stdin $ECR_REGISTRY

            echo "===== Pulling Docker Images ====="
            docker pull $ECR_REGISTRY/$ECR_REPO:$FRONTEND_TAG || exit 1
            docker pull $ECR_REGISTRY/$ECR_REPO:$BACKEND_TAG || exit 1

            echo "===== List Docker Images ====="
            docker images

            echo "===== Deploying Containers ====="
            docker compose --env-file .env up -d --remove-orphans

            echo "===== Running Containers ====="
            docker ps

            echo "===== Cleanup Unused Images ====="
            docker image prune -f











